// an expression is a statement thet returns a value


var additions = 1 + 2

const addition = (a, b) => {
    console.log(a+b)};

addition(2,4)
let names = ['James', 'Peter', 'Mark', 'Jacob', 'Luke', 'John']

function addNames(...names){
    names.push("James")
     console.log(names)
 }
addNames()